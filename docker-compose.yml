services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: publive_tester
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    env_file:
      - backend/.env
    environment:
      # Overrides the host-Postgres value from backend/.env to point at the db service.
      DATABASE_URL: postgres://postgres:postgres@db:5432/publive_tester
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
      # WebRTC live-view signaling (WebSocket). Opt-in via LIVE_VIEW_* in backend/.env.
      - "8001:8001"
      # NOTE (media): WebRTC media uses ephemeral UDP ports that bridge networking does not
      # forward. On the EC2 box, layer docker-compose.webrtc.yml (network_mode: host) and open
      # the UDP range in the security group, OR add a TURN server. See Step 4 of the rollout.
    volumes:
      # Generated specs/tests/reports and the MFA session must survive restarts.
      # capture_session.py needs a headed browser + human OTP entry, so it is run
      # on the host, not in this container — it writes into this same data/ dir.
      - ./data:/app/data
    # Headless Chromium needs more than the default 64MB /dev/shm or it crashes.
    shm_size: '1gb'

  frontend:
    build: ./frontend
    environment:
      VITE_API_PROXY_TARGET: http://backend:8000
    depends_on:
      - backend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules

volumes:
  pgdata:
